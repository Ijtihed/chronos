# Frontier API keys — not used in Phase 0 (all calls go to local Ollama)
# Uncomment and fill when frontier parser is enabled
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Ollama settings (defaults — override if your setup differs)
# OLLAMA_URL=http://localhost:11434
# LOCAL_MODEL=llama3.1:8b
